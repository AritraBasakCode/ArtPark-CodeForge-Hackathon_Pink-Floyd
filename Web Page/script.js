document.getElementById("analyzeBtn").addEventListener("click", () => {
  // Future: validate inputs, upload resume, call extraction + ML APIs, store results
  window.location.href = "skill-gap.html";
});